/*
 * CardJobs Messages
 *
 * This contains all the text for the CardJobs component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.CardJobs';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the CardJobs component!',
  },
});
